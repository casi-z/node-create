{
	"name": "node-create",
	"version": "1.2.0",
	"description": "This file can  create React components and define global variables for the WHOLE  project",
	"main": "create.js",
	"type": "commonjs",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"add": "node create",
		"set": "node set",
		"init": "node init",
		"build": "tsc",
		"run": "node dist/my-app.js",
		"build:watch": "tsc --watch",
		"run:watch": "nodemon dist/my-app.js --watch dist",
		"start": "npm-run-all --parallel build:watch run:watch"
	},
	"author": "casi__z",
	"license": "ISC",
	"dependencies": {
		"prompt-sync": "4.2.0"
	},
	"devDependencies": {
		"@types/colors": "^1.2.1",
		"@types/node": "^18.14.0",
		"nodemon": "^2.0.20",
		"npm-run-all": "^4.1.5",
		"typescript": "^4.9.5"
	}
}
